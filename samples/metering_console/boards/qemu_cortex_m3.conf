# ARM-specific configuration for qemu_cortex_m3
# Enable CMSIS-DSP for spectrum_analyzer_v2 node
CONFIG_CMSIS_DSP=y
CONFIG_CMSIS_DSP_TRANSFORM=y
CONFIG_CMSIS_DSP_STATISTICS=y

# Enable FPU for ARM platform
CONFIG_FPU=y
CONFIG_FPU_SHARING=y
